# Windows-RemoteThread-DLL-Injector
A minimal C++ implementation of a DLL Injection utility for Windows using the CreateRemoteThread technique. The injector forces a target process to load an external DLL by writing the DLL path into the target process's memory
